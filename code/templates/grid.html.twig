<!DOCTYPE html>
<html>
<head>
    <title>Welcome to Symfony!</title>
</head>
<body>
{% for line in grid %}
    {% set counter = 0 %}
    {% for letter in line %}
        {% set counter = counter + 1 %}
        {% if letter == '' %}
            -
        {% else %}
            {{ letter }}
        {% endif %}
        {% if counter == 20 %}
            <p></p>
        {% endif %}
    {% endfor %}
{% endfor %}
</body>
</html>