build:
  nodes:
    coverage:
      tests:
        override:
          - command: docker/scripts/php "-dxdebug.mode=coverage vendor/bin/phpunit --coverage-clover=tests/code-coverage-file"
            coverage:
              file: code/tests/code-coverage-file
              format: clover